@model IEnumerable<Domains.Models.PartBicycle>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuantityRequired)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bicycle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Part)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuantityRequired)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bicycle.BicycleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Part.PartId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PartBicycleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PartBicycleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PartBicycleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
